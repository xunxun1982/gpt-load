/**
 * Search filter utilities for Hub model pool.
 *
 * Note: The ModelPoolTable component implements search filtering inline
 * for better performance and code locality. This file is kept for potential
 * future use if search filtering needs to be shared across multiple components.
 */

// Re-export types for convenience
export type { ModelPoolEntry, ModelPoolEntryV2 } from "../types/hub";
